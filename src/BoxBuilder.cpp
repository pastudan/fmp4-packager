#include "BoxBuilder.h"

// All BoxBuilder methods are inline in the header. This translation unit
// exists so the build system has a target to compile when the static
// library is referenced; if you grow the API beyond inlines, add them here.
